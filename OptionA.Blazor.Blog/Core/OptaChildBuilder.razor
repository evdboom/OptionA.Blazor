@namespace OptionA.Blazor.Blog
@if (Content is not null)
{
    @foreach(var child in Content)
    {
        switch(child.Type)
        {
            case ContentType.Text:
            case ContentType.Quote:
            case ContentType.Paragraph:
            case ContentType.Bold:
            case ContentType.Block:
            case ContentType.Inline:
            case ContentType.Link:
            case ContentType.Italic:
            case ContentType.LineBreak:
                <OptaText Content="(TextContent)child" />
                break;
            case ContentType.QuotePart:
                <OptaQuote Content="(QuotePartContent)child" />
                break;
            case ContentType.Code:
                <OptaCode Content="(CodeContent)child"/>
                break;
            case ContentType.Header:
                <OptaHeader Content="(HeaderContent)child" />
                break;
            case ContentType.Icon:
                <OptaIcon Content="(IconContent)child" />
                break;
        }
    }
}