# Run 4

- Issue: 2
- Role: architect
- Area: (none)
- Model: gpt-5.4
- Backend: copilot-sdk
- Session: devteam-architect-370bb67a7c3e
- Outcome: completed
- Resulting issue status: InProgress

## Summary

No summary provided.

## Skills Used

- brainstorm- plan

## Tools Used

- report_intent- multi_tool_use.parallel- glob- view- rg

## Usage

- Committed credits: 1
- Premium credits: 0
- Tokens: unavailable from backend

## Changed Files

(none)

## Created Issues

- #17
- #18

## Created Questions

(none)

## Output

I’m pulling the existing package layout and prior playground direction into one view so the design can stay aligned with the repo instead of inventing a parallel pattern.I found the repo already has a clear Options/DataProvider + extension-method setup, plus an abandoned blog-builder line. I’m reading the exact package and test patterns now so the issues can target the existing seams instead of proposing a separate architecture.The pattern is consistent: package-level DI extensions, per-component Options/DataProvider pairs, and separate test infrastructure including Playwright. I’m drilling into one representative component and the existing E2E harness so the new package spec can slot in cleanly and stay testable from day one.There’s already a prior architecture artifact in `.devteam` for a `Playground` package. I’m reading that directly and then I’ll turn it into a revised, dependency-ordered execution set that reflects the current ask: interactive docs, live examples, and a tighter migration path away from Blog.Builder.I found a partially generated issue stack from an earlier attempt. Rather than duplicate it blindly, I’m checking what was already proposed so I can either tighten it or supersede the pieces that no longer fit the current scope.I’m checking the existing test/demo apps before locking the host strategy. If they already act as sample hosts, reusing them is a much cleaner path than inventing a new docs app inside this repo.The WebAssembly and Server host apps already register Blog.Builder and shared UI content. That makes the migration seam pretty clear: keep using these hosts for playground examples and E2E coverage, then let the external docs repo consume the reusable package later.One design improvement became obvious while reading: the new editor surface should not hand-roll raw `<input>` controls when the repo already ships `OptionA.Blazor.Components.Direct` input wrappers. I’m checking those now so I can anchor the editor architecture to existing reusable controls.I’ve got enough to tighten the architecture now. One last pass is on registration patterns, because the package should match the exact lifetime/DI style the repo already uses instead of inventing a near-duplicate variant.OUTCOME: completedSUMMARY:
APPROACH:
- **Brownfield decision:** extend the existing OptionA package patterns, **replace** `OptionA.Blazor.Blog.Builder` as the future component-docs authoring path, and **work around** GitHub Pages deployment by keeping this repo focused on a reusable package plus sample/test hosts that an external docs repo can consume.
- **Approach A:** evolve `OptionA.Blazor.Blog.Builder` into the docs tool. Trade-off: reuses an edit/preview loop, but it is tightly coupled to `Post`/JSON/blog content and drag-drop authoring, which is the wrong abstraction for parameter-driven component docs.
- **Approach B (recommended):** add a dedicated `OptionA.Blazor.Playground` Razor class library for live examples, parameter editing, and generated usage markup. Trade-off: a new package, but it matches the actual problem and fits the repo’s current Options/DataProvider conventions.
- **Approach C:** build a full docs framework with Monaco in v1. Trade-off: richer editing, but it adds JS/Roslyn complexity too early and slows delivery of the reusable core.
- **Naming decision:** use `OptionA.Blazor.Playground`. `Interactive` is too broad; `Playground` clearly signals live examples, parameter editing, and preview.

TECH STACK:
- `Microsoft.NET.Sdk.Razor`, `net10.0`
- Package dependencies: `Microsoft.AspNetCore.Components.Web`, `OptionA.Blazor.Components.Direct`
- Unit/component tests: xUnit + bUnit + Moq- Browser tests: existing Playwright project with `OptionA.Blazor.Test` and `OptionA.Blazor.Server.Test`
- **No JavaScript, Monaco, or Roslyn in v1.** If full free-form code editing is needed later, add a separate adapter package such as `OptionA.Blazor.Playground.Monaco` so the core package stays JS-free and simple.

PROPOSED STRUCTURE:
```textOptionA.Blazor.Playground/
 ServiceCollectionExtensions.cs _Imports.razor OptAPlayground.razor OptAPlayground.razor.cs OptAPlaygroundPreview.razor OptAPlaygroundPreview.razor.cs OptAPlaygroundEditor.razor OptAPlaygroundEditor.razor.cs OptAPlaygroundCode.razor OptAPlaygroundCode.razor.cs Editors/
 OptAPlaygroundTextEditor.razor(.cs)
 OptAPlaygroundNumberEditor.razor(.cs)
 OptAPlaygroundBooleanEditor.razor(.cs)
 OptAPlaygroundEnumEditor.razor(.cs)
 OptAPlaygroundSelectEditor.razor(.cs)
 OptAPlaygroundColorEditor.razor(.cs)
 OptAPlaygroundContentEditor.razor(.cs)
 Struct/
 PlaygroundDescriptorBase.cs PlaygroundDescriptor.cs PlaygroundParameterDescriptor.cs PlaygroundChoice.cs ParameterEditorType.cs PlaygroundLayout.cs PlaygroundOptions.cs OptAPlaygroundOptions.cs IPlaygroundDataProvider.cs PlaygroundDataProvider.cs IPlaygroundParameterBinder.cs PlaygroundParameterBinder.cs IPlaygroundMarkupFormatter.cs PlaygroundMarkupFormatter.cs wwwroot/
 playground.cssOptionA.Blazor.Playground.UnitTests/
 GlobalUsings.cs Descriptors/
 Services/
 Components/

OptionA.Blazor.Test.Shared/
 Pages/Playground/
 ButtonsPlayground.razor(.cs)
 TabsPlayground.razor(.cs)
 Struct/Playground/
 IPlaygroundExampleCatalog.cs PlaygroundExampleCatalog.cs```

KEY DECISIONS:
- `PlaygroundDescriptorBase` is the runtime contract; `PlaygroundDescriptor<TComponent>` is authoring sugar.
- Use a **manual descriptor model**, not reflection or source generation, so examples stay explicit, WASM-friendly, and easy to test.
- Editor components should reuse `OptionA.Blazor.Components.Direct` inputs (`OptAInputText`, `OptAInputInteger`, `OptAInputCheckbox`, `OptASelect<T>`, `OptAEnumSelect<TEnum>`) instead of raw HTML inputs.
- `IPlaygroundParameterBinder` owns all non-trivial conversion logic: defaults -> editable state, editable state -> `DynamicComponent` parameters, and content values -> `RenderFragment`. Keep it pure and constructor-injected.
- `IPlaygroundMarkupFormatter` owns generated Razor markup output. Keep it pure and constructor-injected.
- `OptAPlayground.razor.cs` should remain orchestration-only: store state, call injected services, and render Preview/Editor/Code children.
- Service registration should mirror existing package conventions exactly: `AddOptionAPlayground` and `AddOptionABootstrapPlayground`, supporting `Singleton` and `Scoped` only via `TryAddSingleton` / `TryAddScoped`.
- Reuse the existing WebAssembly and Server sample apps as the first integration surface. Do not create a new docs host in this repo.

RATIONALE:
This gives the repo a component-focused docs primitive instead of stretching the blog builder into a role it does not fit. It also keeps the initial scope incremental: a reusable package, stable test seams, existing host apps for validation, and a clean path for the external docs repo to consume the result later.

EXECUTION ORDER:
1. Keep existing issues `0007`-`0015` as the base execution chain.
2. Revise the handoff so `0007` includes `IPlaygroundParameterBinder`, `IPlaygroundMarkupFormatter`, and `PlaygroundChoice`.
3. Treat `0010`-`0012` as consumers of those injected services and of `OptionA.Blazor.Components.Direct`.
4. Treat `0015` as external-consumer readiness (package/readme/CI), not GitHub Pages deployment.
5. Add the missing host-app and browser-verification work below.

ISSUES:
- role=developer; area=playground-host; priority=86; depends=7,9,10,11,12,13; title=Expose playground examples in shared host apps; detail=Reuse the existing sample hosts instead of creating a new docs app. Update `OptionA.Blazor.Test\Program.cs`, `OptionA.Blazor.Server.Test\Program.cs`, `OptionA.Blazor.Test.Shared\OptionA.Blazor.Test.Shared.csproj`, `OptionA.Blazor.Test.Shared\Shared\MainLayout.razor`, and add `OptionA.Blazor.Test.Shared\Pages\Playground\ButtonsPlayground.razor(.cs)` plus `TabsPlayground.razor(.cs)`. Register `AddOptionABootstrapPlayground()`, add a single top-level Playground navigation group, and render descriptor-backed examples for at least Buttons and Tabs. Introduce `OptionA.Blazor.Test.Shared\Struct\Playground\IPlaygroundExampleCatalog.cs` and `PlaygroundExampleCatalog.cs` so example metadata is injected rather than hard-coded. Keep both `Program.cs` files bootstrap-only and keep each example page focused on one component family.
- role=tester; area=playground-host; priority=78; depends=15; title=Add browser coverage for playground interactions; detail=Extend `tests\OptionA.Blazor.E2E` with a dedicated page object and WebAssembly/Server tests for the new playground pages. Add `tests\OptionA.Blazor.E2E\PageObjects\PlaygroundPage.cs` and matching test files under `tests\OptionA.Blazor.E2E\Tests\`. Assert that the playground page loads, the preview renders the expected component root attribute (`[opta-button]` or `[opta-tabs]`), changing a text/boolean/select editor updates preview state, and the generated code block changes from the default snippet after the edit. Keep direct Playwright calls inside page objects and prefer awaited DOM assertions over timing-only waits.

SKILLS_USED:
- brainstorm- planTOOLS_USED:
- report_intent- multi_tool_use.parallel- glob- view- rgQUESTIONS:
(none)

## Error

