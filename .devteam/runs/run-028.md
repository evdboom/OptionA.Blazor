# Run 28

- Issue: 46
- Role: devops
- Area: blog-builder-retirement
- Model: gpt-5.4
- Backend: copilot-sdk
- Session: devteam-devops-14a8a782601f
- Outcome: completed
- Resulting issue status: InProgress

## Summary

Updated `.github/workflows/publish-nuget.yml` so `OptionA.Blazor.Blog.Builder.csproj` is treated as a retired package and excluded from `Is-PackableProject` selection. That keeps the project buildable in the repo while preventing it from being auto-selected, packed, published, or attached to future NuGet releases. The local packable-project check no longer lists Blog.Builder, and `dotnet build .\OptionA.Blazor.sln --configuration Release` completed successfully.

## Skills Used

(none)

## Tools Used

- view- rg- apply_patch- powershell- `git --no-pager status --short`
- `git --no-pager diff -- .github/workflows/publish-nuget.yml`
- `dotnet build .\OptionA.Blazor.sln --configuration Release`

## Usage

- Committed credits: 1
- Premium credits: 0
- Tokens: unavailable from backend

## Changed Files

(none)

## Created Issues

(none)

## Created Questions

(none)

## Output

I’m checking the release workflow and the Blog.Builder project so I can make the narrow CI change without widening scope.I found the workflow path and there are unrelated workspace changes, so I’m keeping this edit limited to the publish workflow and the issue record for scope context.I have the scoped issue context; next I’m checking whether there’s already a release-exclusion pattern to reuse before patching the workflow.I found no existing exclusion mechanism, so I’m making the workflow explicitly treat Blog.Builder as a retired package that stays buildable but never enters the release matrix.The workflow patch is in; I’m validating the exact exclusion behavior and then doing the required build pass.I’ve confirmed the retired package drops out of the release candidate list; I’m pulling the exact diff for the handoff summary.OUTCOME: completedSUMMARY:
Updated `.github/workflows/publish-nuget.yml` so `OptionA.Blazor.Blog.Builder.csproj` is treated as a retired package and excluded from `Is-PackableProject` selection. That keeps the project buildable in the repo while preventing it from being auto-selected, packed, published, or attached to future NuGet releases. The local packable-project check no longer lists Blog.Builder, and `dotnet build .\OptionA.Blazor.sln --configuration Release` completed successfully.

ISSUES:
(none)
SKILLS_USED:
(none)
TOOLS_USED:
- view- rg- apply_patch- powershell- `git --no-pager status --short`
- `git --no-pager diff -- .github/workflows/publish-nuget.yml`
- `dotnet build .\OptionA.Blazor.sln --configuration Release`
QUESTIONS:
(none)

## Error

