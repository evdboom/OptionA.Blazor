@page "/gallery-modal"

<OptAGallery MaxThumbnailContainerHeight="50vh"
             ShowTitleOnThumbnail="true"
             Mode="GalleryMode.Modal"
             ThumbnailsPerRow="4"
             ThumbnailsPerRowMargin="10"
             MaxModalWidth="500px">
    <Images>
    @for(int i = 1; i <= 10; i++)
    {
        <OptAGalleryImage ImageNumber="@i"
                          ImageText="@i.ToString()"
                          ImageThumbnailUrl="@GetUrl(i)"
                          ImageUrl="@GetUrl(i)"/>
    }
    </Images>
</OptAGallery>